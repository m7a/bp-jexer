`mdvl-jexer` package for building this library:
https://gitlab.com/klamonte/jexer.git/
Given a working `ant` command, necessary dependencies are automatically
retrieved and stored in directories next to `bp-jexer`.
